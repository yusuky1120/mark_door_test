services:
  web:
    build: .
    container_name: markdoor_todo_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload